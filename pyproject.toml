# pyproject.toml — Configuration du paquet Python réutilisable mazegen.
# Définit les métadonnées du paquet : nom (mazegen), version, auteurs, description,
# licence, version Python minimale requise, et dépendances.
# Configure l'outil de build (setuptools ou hatchling) pour empaqueter
# le module mazegen en .whl et .tar.gz.
# Contient également les sections de configuration pour mypy, flake8 et pytest
# afin de centraliser tous les paramètres d'outils en un seul fichier.
