# Fichier de configuration par défaut du labyrinthe.
# Contient les paires CLE=VALEUR définissant les paramètres de génération :
# dimensions du labyrinthe (WIDTH, HEIGHT), coordonnées d'entrée (ENTRY) et de sortie (EXIT),
# nom du fichier de sortie (OUTPUT_FILE), mode parfait (PERFECT), graine aléatoire (SEED)
# et algorithme de génération (ALGORITHM).
# Les lignes commençant par # sont des commentaires ignorés par le parseur.
# Ce fichier sert d'exemple et est utilisé par défaut lors des tests et démonstrations.

WIDTH=60
HEIGHT=60
ENTRY=0,0
EXIT=59,59
OUTPUT_FILE=maze.txt
PERFECT=False
ALGORITHM=Kruksal
PLAYABLE=True