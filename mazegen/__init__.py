# mazegen/__init__.py — Initialisation du paquet réutilisable mazegen.
# Expose la classe MazeGenerator comme interface publique du module.
# Ce fichier est le point d'entrée du paquet pip installable.
# Contient la docstring de module décrivant l'usage global du paquet,
# la version et les informations d'auteurs.
